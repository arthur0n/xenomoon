# Godot Sound Effects — Audio Manager / voice-cap pattern — transcript digest

**Source** — `godot-sound-effects.md` (now in `transcripts/archive/`). Hexagod dev (Chess Survivors author); YouTube SFX/audio-manager walkthrough, GitHub link in vid.
**Why harvested** — we are about to build SOUND EFFECTS for the game (wire SFX onto existing seams; gen_sfx.gd placeholders already exist).
**Assumed build context** — FPS POC, Godot 4.6, strict-typed GDScript, data-driven `.tres`, existing `tools/gen_sfx.gd` + `AudioStreamPlayer3D` on enemies, skill `godot-audio` already authored.

**Points**

| #   | Point (technique/claim)                                                                                                           | Valid for our stack?   | Already learned?      | Where / gap                                                                                                                                                                                                                                                       | Verdict                                    |
| --- | --------------------------------------------------------------------------------------------------------------------------------- | ---------------------- | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| 1   | "Sound spiking" = N copies of one SFX play same frame → blown-out top end / distortion; THE problem to solve                      | holds                  | gap                   | godot-audio handles overlap-CUTOFF (max_polyphony) but NOT the inverse (too many at once). Distinct issue.                                                                                                                                                        | act — design problem worth naming          |
| 2   | A global `AudioManager` autoload (Node2D) with `create_audio_at_location(pos, type)` API                                          | conflicts              | covered (as rejected) | godot-audio convention: **NO autoload AudioManager** — players are entity component children, fired at the seam. CLAUDE.md godot-audio line + skill "No autoload".                                                                                                | skip — conflicts our convention            |
| 3   | Per-sound `SoundEffectSetting` custom Resource (limit, type enum, file, volume, pitch_scale, pitch_randomness) authored in editor | holds with caveat      | partial               | Matches our data-driven `.tres` default. godot-audio uses Inspector-set `stream`/`bus` on the node, NOT a typed SFX Resource catalog. Our `godot-resource-registry` could index such a family. Caveat: drop the enum, key by StringName id (registry convention). | improvement — data-driven SFX def is a gap |
| 4   | Voice-cap: track active instances per sound, suppress new `play()` at limit, decrement on `finished`                              | holds with caveat      | gap                   | Nothing in godot-audio caps concurrent COUNT. Caveat: implement per-entity/per-sound, NOT via a cross-entity autoload (composition).                                                                                                                              | act — the real reusable technique          |
| 5   | Memory-leak fix: connect spawned player's `finished` → `queue_free`                                                               | holds                  | covered               | godot-audio step 4 despawn pattern already does `finished.connect(queue_free)` (idempotent-guarded).                                                                                                                                                              | skip — already have it, better             |
| 6   | Per-play pitch randomness to vary repeated sounds                                                                                 | holds                  | partial               | godot-audio pool sketch passes `pitch_scale` but no randomness range. Cheap juice for our gen_sfx placeholders.                                                                                                                                                   | improvement — small add                    |
| 7   | Source SFX from zapsplat (free CC0-ish license), edit in Audacity                                                                 | out of scope (process) | covered               | Sourcing is the asset-advisor loop + `assets/audio/` path; gen_sfx.gd already makes placeholders.                                                                                                                                                                 | skip                                       |
| 8   | Ship quieter + give player a volume slider; use contrast for impact moments                                                       | out of scope           | covered (parked)      | godot-audio "Parked": volume settings menu / options UI out for whole roadmap.                                                                                                                                                                                    | skip — parked                              |

**Recommended next** — gaps to act on now:

- Voice-cap / anti-spike technique (point 1+4) → **skill-researcher**: no `godot-*` skill caps concurrent SFX COUNT; check if a per-entity/per-sound voice limiter (composition, no autoload) exists to adopt, else flag as a godot-audio extension. The genuine gap for this build.
- Data-driven SFX definition Resource + registry (point 3) → **game-designer** (small): decide whether SFX should be authored as `.tres` (volume/pitch/cap/stream) keyed by StringName via `godot-resource-registry`, vs Inspector-set on the node per godot-audio. Architecture call before wiring many sounds.

**Later** (valid, not needed this iteration):

- Per-play pitch randomness (point 6) — fold into godot-audio's pool when juicing; not blocking.
- (System/framework) godot-audio could gain an explicit "anti-spike voice-cap" section + Error→Fix row ("dozens of same SFX same frame → distortion") — park as a framework skill-improvement, route via skill-researcher's normal flow, not game work.
