# tools — index

<!-- Generated by `npm run check:library -- --write` — do not edit by hand. -->

- [scene feedback (render frame + capture run output) — tool definition](feedback.md) — build thin. ~80% already exists in tools/verify_render.gd (boots a scene, warms up frames, calls img.save_png(...)). The build is a thin wrapper + small lifts, NOT a new renderer:
- [game-observe — tool definition suite](game-observe.md) — Build thin — a three-line shell wrapper capturing a running game frame + log for agent observation.
- [GDScript linter — gdstyle (in-editor) over gdlint (gate)](gdscript-linter.md) — Verdict: Adopt gdstyle as the in-editor, config-driven GDScript linter (the
- [graphify for GDScript — tool definition](graphify-gdscript.md) — use as-is for design/concept graph; GDScript AST coverage is partial (symbols
- [scene-screenshot — tool definition](scene-screenshot.md) — build thin. verify_render.gd already contains the correct warmup loop and save_png call, but samples root.get_texture() (the window viewport) rather than the render target that actually holds the 3D …
- [tscn sibling name clash lint — tool definition](tscn-name-clash-lint.md) — Build thin. No MIT tool exists for this. The parse is ~30 lines of Python:
- [input-driven render assertion — tool definition](verify-render-action.md) — build thin. Pattern is a direct extension of capture_screenshot.gd (already
