{
  "name": "webapp",
  "label": "Web App",
  "description": "Head-start package for React + Node.js web apps. Ships a proven, human-gated, issue-driven pipeline (design PRD → analyze → implement → QA → adversarial review → auto-commit) with /feedback /design /analyze /implement /qa /audit /commit /build /uat, then learns each project's conventions on top.",
  "populated": true,
  "engine": {
    "name": "node",
    "projectFile": "package.json"
  },
  "inventory": {
    "scenes": [],
    "scripts": [".js", ".ts", ".jsx", ".tsx"],
    "ignore": ["node_modules", "dist", "build", "out", "coverage", ".next"]
  },
  "materializeIntoProject": false,
  "plugin": "domains/webapp/plugin",
  "orchestrator": "domains/webapp/orchestrator.md",
  "builders": ["developer"],
  "commands": {
    "build": "npm run build --if-present",
    "lint": "npm run lint --if-present",
    "test": "npm test --if-present",
    "smoke": "npm run smoke --if-present",
    "e2e": "npm run e2e --if-present"
  }
}
