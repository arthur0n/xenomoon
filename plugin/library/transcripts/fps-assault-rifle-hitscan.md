# How to Make an Assault Rifle in Godot — transcript digest

**Source** — `godot-bullets.md` (the raw, now in `transcripts/archive/godot-bullets.md`). A short FPS "assault rifle" tutorial — builds a gun mesh on the camera, an animated fire loop, **raycast/hitscan** damage, and camera shake. Assumes its own FPS-controller and crosshair videos.
**Why harvested** — we are about to build phase **A3** of `docs/roadmap/fps_poc.md`: projectile spawn → move → despawn + fire-rate cooldown.

**Heads-up on the source's fit** — this video teaches a **hitscan** (raycast) weapon, _not_ a travelling-projectile weapon. A3's gate is explicitly "projectiles travel and despawn", so the video's core mechanic is a different design than what we're building. It is still useful for the _firing scaffolding_ (input, muzzle placement, fire-rate, hit reaction, feel). Its hitscan-vs-projectile mismatch is the headline finding, not an oversight.

**Points**

| #   | Point (technique/claim)                                                                                                                                                                                            | Valid for our stack?                                                                                                                                                                                                | Already learned?    | Where / gap                                                                                                                                    | Verdict                                             |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| 1   | Gun rig: child a `Node3D` "Hand" + `MeshInstance3D` gun under the camera so the weapon moves with the view; position it via the camera preview                                                                     | holds with caveat — our eye-camera lives on the `Head` child inside the SubViewport (`godot-first-person-controller`); attach the weapon under `Head`/`Camera3D`, not a bare camera                                 | gap                 | FPC skill owns the body/Head/eye-camera rig but does not document mounting a weapon/muzzle on it                                               | gap (small)                                         |
| 2   | Add a `fire` input action in the Input Map; gate firing on `Input.is_action_pressed("fire")`                                                                                                                       | holds with caveat — our convention names the action **`shoot`** (FPC skill calls it an "A3 action"), not `fire`; A3 must add it to the map + CLAUDE.md                                                              | partial             | CLAUDE.md "Input actions" lists move\_\*/jump/cycle_level — `shoot` not yet added; FPC skill defers it to A3                                   | partial (convention)                                |
| 3   | Drive the gun with an `AnimationPlayer` fire clip (non-looping recoil keyframes); set **fire rate by the animation playback speed**, and run once-per-loop logic in the single frame where `not anim.is_playing()` | holds, but **anti-pattern for A3** — A3's contract is an explicit one-shot **`Timer` cooldown** (deterministic, typed, testable), not "fire rate = animation length / `is_playing()` edge"                          | gap                 | no skill covers a fire-rate gate; the cleaner Timer approach is the one our roadmap+sibling digest already prescribe (fps-survivor-arena pt 6) | gap — prefer Timer over animation-driven cadence    |
| 4   | Damage via **hitscan**: `RayCast3D` child of camera (cast e.g. `(0,0,-200)`), `is_colliding()` → `get_collider()`, if `is_in_group("enemy")` subtract `damage` from its health                                     | holds technically, but **diverges from A3** — A3 spawns a _travelling projectile_, not an instant ray; hitscan is a different weapon design                                                                         | gap (and off-track) | no skill covers either hitscan or projectile; raise the design choice rather than silently building the ray                                    | gap + **design divergence** (hitscan vs projectile) |
| 5   | Hit reaction: target uses a `health` field and an `enemy` group; reaches 0 → dies                                                                                                                                  | holds with caveat — composition-friendly, but typed GDScript + a Health _component_ (signals up) fits our `godot-composition`/`godot-code-rules`, not a loose `health` var with duck-typed access                   | gap                 | B2 (targets) territory; no skill, and `godot-composition` covers structure generically only                                                    | gap (B2, not A3)                                    |
| 6   | Camera shake: `lerp` the camera translation toward a small `randf_range` offset each fire loop, reset to zero when not firing; doubles as bullet spread                                                            | holds with caveat — mutating the eye-`Camera3D` translation fights the FPC rig (camera sits at Head origin, identity transform); shake belongs on `Head`/an offset node, not the camera, and is **out of A3 scope** | gap                 | not covered; juice/feel, parked                                                                                                                | Later (out of A3 scope)                             |
| 7   | Swap the cube for a real AK `.glb`, add muzzle flash / bullet-hole VFX                                                                                                                                             | out of scope for A3 — VFX beyond the outline pass is explicitly **out of scope** in the roadmap; model swap is the asset-advisor loop later                                                                         | out of scope        | roadmap "Out of scope"; `godot-mesh-import-pixel-art` would own a real weapon model later                                                      | out of scope                                        |

**Recommended next** — gaps A3 needs now:

- Projectile **spawn → move → despawn + fire-rate `Timer` cooldown** is still the one genuine A3 gap and no `godot-*` skill covers it. **But a buy-vs-build gate is already open** (addon-researcher is searching for a free projectile/weapon addon, verdict pending) and skill-researcher already REJECTED adopting an external skill. → **hold for the addon-researcher verdict first**; only re-dispatch **skill-researcher** to author a `godot-projectile` skill if the addon search comes back empty. This digest deliberately does not pre-empt that gate.
- **Hitscan vs projectile** is a real design question this video forces: the source teaches hitscan, A3's gate demands a travelling projectile. → **game-designer** to confirm A3 stays projectile (it should, per the gate) and note hitscan as an explicitly-not-now alternative, so godot-dev doesn't build the ray by reflex.
- Add the **`shoot`** input action (mouse click) to the Input Map + CLAUDE.md "Input actions" line → orchestrator convention edit, surfaced via the A3 game-designer scope (FPC skill already reserved the name).

**Later** (valid but not needed to stand up A3):

- Animation-driven recoil + camera/Head shake "juice" (pts 3, 6) — feel polish after the projectile mechanic works; do shake on Head, not the eye-camera.
- Target `health` + hit reaction (pt 5) — that's **B2 (targets/enemies)**; scope it then as a Health _component_, not a loose var.
- Real weapon `.glb` + muzzle/impact VFX (pt 7) — out of POC scope; asset-advisor loop if ever revisited.
