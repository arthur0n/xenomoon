# 5 INVALUABLE Godot 4 Code Patterns (feat. GDQuest) — transcript digest

**Source** — `5-invaluable-patterns.md` (raw now in `transcripts/archive/5-invaluable-patterns.md`). YouTube "5 INVALUABLE Godot 4 Code Patterns (feat. @Gdquest)".
**Why harvested** — building/benchmarking code-quality best practices; feeds the internal code-quality evaluator agent (4 axes: clean architecture / reusable systems / proper state management / optimized code; descriptive rubric, split Godot-4-only + general sub-rubrics).

**Points**

| #   | Point (technique/claim)                                                                                                                                                                                                                                                                                                        | Valid for our stack?                        | Already learned?                                                         | Where / gap                                                                                                                                                                                                                                             | Verdict                                                   | Axis / sub-rubric                                                                                                  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| 1   | **Command pattern via first-class `Callable`** — pass a fn as arg/return/var (`Callable`, `Callable.call/callv`), wrap into a command object; e.g. a reusable `call_delayed(callable, args, delay)` instead of inline Timers everywhere.                                                                                       | holds                                       | gap (DRY/static-util convention exists but no command/Callable guidance) | godot-code-rules DRY → `tools/lib/` static util is the natural home; no skill teaches Callable-as-command                                                                                                                                               | Rubric item + Later                                       | reusable systems + optimized code / **Godot-4 sub-rubric** (Callable is engine API)                                |
| 2   | **Global signal bus = `Node` autoload with signals** — decouple emitter scene from unrelated listener scene (mob `died(score)` → UI) via an `Events` autoload, connect in `_ready`.                                                                                                                                            | **conflicts**                               | covered (we forbid it)                                                   | CLAUDE.md "no autoload sneak-in" / godot-composition anti-pattern row "Autoload used to share behavior between entities → component scene"; our combat decoupling = `died(enemy)` signal + duck-typed seam (godot-fps-enemy-combat), NOT a bus          | Anti-pattern to encode                                    | clean architecture + state mgmt / **Godot-4 sub-rubric**                                                           |
| 3   | **Object pooling** — reuse+reset nodes instead of instance/free churn; pool keyed by scene path/PackedScene; `request_ready()`, manual non-`_ready` state reset, play RESET anim, return-to-pool. Note: Godot refcounts so MANY games don't need it; only high-churn (bullet-hell).                                            | holds with caveat                           | partial                                                                  | godot-travelling-projectile-3d (despawn) + godot-oneshot-vfx (fire-and-free leak) + godot-decal-vfx (round-robin **pool** of N reused slots) — we pool decals but no general node-pool skill; POC churn is low so caveat = not needed now               | Later + rubric nuance ("pool only when churn proven")     | optimized code + state mgmt / **general sub-rubric** (pooling is engine-agnostic concept; reset specifics Godot-4) |
| 4   | **Nodes as components, `get_parent()`/`owner` introspection** — child node inspects parent type to act as a component; `owner` points at scene root so a child can drive the root (state machine).                                                                                                                             | **holds with caveat / partially conflicts** | covered                                                                  | godot-composition rule 3 "signals up, calls down; a component never does `get_parent().something()`" — we ALLOW `owner.is_on_floor()` style for state-machine-on-root (matches video's owner use), but FORBID `get_parent()` type-sniffing for behavior | Anti-pattern (get_parent sniff) + allowed pattern (owner) | clean architecture / **Godot-4 sub-rubric**                                                                        |
| 5   | **Data as Resources or CSV tables, not code** — author game data (upgrades/items/spells) as `class_name` Resources (export vars, in-editor) or CSV parsed at load; reflection via `set(header, value)` + `typeof()` cast; optional dynamic `load()` of a `.gd`/`.gdc` by matching name. JSON = good transport, bad data store. | holds (caveat on reflection/dynamic-load)   | gap                                                                      | no skill covers data-as-Resource/CSV; our `@export` data-variant rule (composition rule 7) is the seed but scoped to variants, not data tables. Reflection `set()` + dynamic `load()` skirt strict-typed GDScript (Variant) → flag                      | Rubric item + Later                                       | reusable systems + clean architecture / **Godot-4 sub-rubric** (Resource/CSV) ; general (data-driven design)       |

**Already-learned tally** — covered 2 (#2, #4), partial 1 (#3), gap 2 (#1, #5).

**Conflicts (named convention)** — #2 global signal-bus autoload conflicts CLAUDE.md "no autoload sneak-in" + godot-composition anti-pattern ("autoload to share behavior → component scene"). Our one autoload `RunStateSingleton` is shared _state_, allowed; behavior bus is not. #4 `get_parent()` type-sniff conflicts godot-composition rule 3 (the `owner`-drives-root half is permitted).

**Rubric items for the evaluator agent**

- **Anti-pattern (Godot-4):** autoload signal-bus for cross-scene behavior decoupling → justify against component-scene + signal-up. (axis: clean architecture)
- **Anti-pattern (Godot-4):** component `get_parent()` type introspection for behavior → signals-up / `@export` injection. `owner`-drives-root is the _allowed_ form — descriptive distinction. (axis: clean architecture)
- **Principle (general):** pool only when instance/free churn is _measured_ high (bullet-hell); refcounting means default = no pool. Premature pooling = smell. (axis: optimized code)
- **Principle (Godot-4):** game data lives in Resources / CSV tables, not hardcoded — data-driven over code. Reflection `set()`/dynamic `load()` cost strict typing; justify. (axis: reusable systems)
- **Principle (Godot-4):** Callable-as-command / `call_delayed`-style helper over scattered inline Timers — DRY reusable seam in `tools/lib/`. (axis: reusable systems)
- **Cross-cut:** JSON = transport not data store. (axis: clean architecture)

**Recommended next** — nothing to dispatch for a _build_ now (POC churn low, no data-table/Callable-command feature queued). Items #1, #5 are rubric fodder, handed to the evaluator-agent author (t409), not skill-researcher yet.

**Later**

- #1 Callable-as-command / `call_delayed` util → skill-researcher IF a delayed-call/command need recurs in build.
- #3 general node object-pool → skill-researcher IF a high-churn system (bullet-hell spawner) lands; today decal/vfx pooling suffices.
- #5 data-as-Resource/CSV table system → addon-researcher (CSV importer) or skill-researcher IF upgrades/items data scales past `@export` variants.
