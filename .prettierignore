node_modules/
logs/
package-lock.json

# Hand-tuned markup — keep prettier off it for now (mirrors lint-staged).
*.html

# CLI-authored token history (npm run token-history) — sole author is token-history.js;
# prettier would re-pad the trend table that the CLI un-pads on the next render (churn loop).
.claude/token-audits/history.json
.claude/token-audits/history.md

# Generated library kind-indexes (sole author is gen-library-index.js) — prettier escapes
# markdown glob chars the generator writes literally (`godot-*` → `godot-\*`), so on the next
# `npm run check:library` the index reads as stale and fails validate. Same churn loop as above;
# keep prettier off them so the generator's output stays canonical.
plugin/library/*/index.md
