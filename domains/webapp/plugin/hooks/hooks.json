{
  "description": "Webapp domain hooks. The pipeline's deterministic gates now live in CORE (the commit gate and its label stamp); push, dependency and branch-creation JUDGEMENT lives in the session's permission layer as per-project policy, not here \u2014 those are decisions for a human, not conditions a script can settle.",
  "hooks": {}
}
