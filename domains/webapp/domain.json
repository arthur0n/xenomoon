{
  "name": "webapp",
  "label": "Web App",
  "description": "Head-start package for React + Node.js web apps. Ships a proven, human-gated, issue-driven pipeline (bug-triage → senior-dev → developer) with /feedback /triage /solution /implement /build, then learns each project's conventions on top.",
  "populated": true,
  "engine": {
    "name": "node",
    "projectFile": "package.json",
    "needsBinary": false
  },
  "inventory": {
    "scenes": [],
    "scripts": [".js", ".ts", ".jsx", ".tsx"],
    "ignore": ["node_modules", "dist", "build", "out", "coverage", ".next"]
  },
  "materializeIntoProject": false,
  "plugin": "domains/webapp/plugin",
  "orchestrator": "domains/webapp/orchestrator.md",
  "commands": {
    "build": "npm run build --if-present",
    "lint": "npm run lint --if-present",
    "test": "npm test --if-present"
  }
}
