# Framework self-audit ledger

Read FIRST, append AFTER. Three commands write findings here — `/framework-audit` (cold spine scan),
`/framework-feedback` (distil the just-finished conversation), and `/harvest-sessions` (mine past
session logs for recurring friction); `/framework-audit-fix <ids>` reads them, applies the agreed
ones, marks them `done`. **The ledger is EPHEMERAL working state, not
a history log: once a pass fully resolves, prune it back to the "Last audit" line below — the fixes
live in the files + git, not here.** Only keep rows that are still `open`/`later`.

Dimensions: **D1** agents over the index cap · **D2** skill/agent game-path contamination · **D3**
name↔scope (+ paradigm/genre fit) · **D4** data-driven orientation · **D5** prompt bloat/duplication
· **D6** orchestrator · **D7** the framework's own commands · **D8** verification flow · **D9**
harness load-bearing under the current model (strip stale scaffolding / harden fuzzy→tool).

Buckets (skill-researcher convention): **3** no-brainer · **4** improvement · **5** system/later ·
**6** skip. Verdict on top: `fix-now` (3/4) · `later` (5) · `skip` (6). Status: `open` · `done <date>` · `skip`.

## Entry template

```
### <YYYY-MM-DD> — dimensions: <D1,D2,...>
| id | bucket | finding + proposed fix | verdict | status |
|----|--------|------------------------|---------|--------|
| D2-<slug> | 3 | <one line> | fix-now | open |
Process note: <one tweak to this command / ledger, or `none`>
```

## Last audit

**2026-07-01** — weekly cold scan (D1,D4 clean) + human-steered root-cause deep-dive on the promotion/authoring seam. Applied+pruned this pass: `D2-enemy-ai-codenames`, `D7-scope-stale-four`, `D7-fix-targets-plugin-only` (record → git). Pruned all resolved rows per the prune-on-done rule; only `open`/`later`/`skip`-tombstone rows remain below.

Parking (dimension ideas, unactioned): a **skill-content-gap** dimension (a skill missing a known technique/caveat maps to no clean D1–D9); a **D10 "seam integrity"** dimension (do the framework's own gates — promote/materialize/validate — enforce the conventions its docs state?). Both surfaced from real friction; fold into `framework-audit.md` if a second instance appears.

### 2026-06-26 — dimensions: D2,D3,D5,D6,D7,D8 (D1,D4 clean)

| id                       | bucket | finding + proposed fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | verdict | status |
| ------------------------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------ |
| D8-verify-gate-gap       | 3      | godot-enemy/-ranged-combat/-vfx list godot-verify in skills but have NO `## Verification (mandatory)` block (godot-dev/player/assets/visuals do) — add it                                                                                                                                                                                                                                                                                                                                                     | fix-now | open   |
| D8-verify-crossref       | 3      | godot-verify:147 re-teaches runtime-smoke's job inline — add "see godot-runtime-smoke" pointer                                                                                                                                                                                                                                                                                                                                                                                                                | fix-now | open   |
| D7-library-contradiction | 4      | framework-audit.md :34/:159 ("game-specifics belong in plugin/library/") contradicts :70-72 ("AGNOSTIC records only") — rewrite to agnostic-only (game facts → game-local)                                                                                                                                                                                                                                                                                                                                    | fix-now | open   |
| D7-token-audit-ships     | 4      | token-audit.md ships via plugin to every game but mines forge logs/ + cites forge ui/ internals — move to .claude/commands/ or strip forge premise                                                                                                                                                                                                                                                                                                                                                            | fix-now | open   |
| D8-smoke-misassigned     | 4      | godot-runtime-smoke (teaches AUTHORING tools/smoke\_\*.gd) scoped to bug-triage, which is forbidden to write game code/tools — move to seam builders (godot-dev/-enemy/-ranged-combat/-player)                                                                                                                                                                                                                                                                                                                | fix-now | open   |
| D5-research-intake       | 6      | NOT a clean extraction (verified): unlike the 6-bucket (100% verbatim ×4), the shared core is only ~2 sentences (Hermes-intake principle + forge-facts rule) and each researcher tailors it (own output artifact: skill / library/addons / library/tools; own spot-check examples; search vs scout). Partial dedup at best + always-needed → inline (reliability > DRY). Fails the verbatim+multi-consumer bar that made research-presenting a clean win                                                      | skip    | skip   |
| D2-blockout              | 4      | pixel-lighting:21,30,43,61 asserts `blockout_01.tscn` "already carries the rig" as existing fact — teach rig values directly                                                                                                                                                                                                                                                                                                                                                                                  | fix-now | open   |
| D2-roadmap               | 4      | pixel-lighting:25,79,86 cites `docs/roadmap/first_game.md`/Phase-5 gate as scope authority — give agnostic technical (pixel-readability) rationale                                                                                                                                                                                                                                                                                                                                                            | fix-now | open   |
| D2-library-records       | 4      | shipped `plugin/library/addons/cyclops-level-builder.md` + `tools/scene-screenshot.md` are game-COUPLED throughout (scene names + `Main/SubViewportContainer/SubViewport`, 640x360, furniture incident, pixel-art/orthographic rig), yet `plugin/library/` is "AGNOSTIC-only". Scene-name-only sweep is cosmetic — DECIDE: (a) genericize the whole record keeping the reusable verdict/caveat, or (b) move game-local. Likely a pattern across all of library/{addons,tools}. Surfaced post-D2-apartment fix | fix-now | open   |
| D5-asset-advisor-bloat   | 4      | asset-advisor (largest agent) restates per-medium facts 4x — collapse Classification+Checklist into one table, trim 2 worked examples (~25-30%)                                                                                                                                                                                                                                                                                                                                                               | fix-now | open   |
| D3-ranged-combat         | 4      | agent named "ranged-combat" owns godot-effect-composition (covers melee/traps/buffs/dots) — name mis-signals routing; consider rename (godot-weapons-abilities). Weigh churn (just split)                                                                                                                                                                                                                                                                                                                     | fix-now | open   |
| D8-smoke-bloat           | 5      | godot-runtime-smoke 425L = 3-4 capabilities (smoke-author + input-bot playthrough + log-capture + navigability) — split input-bot to own skill, demote machine-verified empirics to library/findings/; sweep its firing_yard/cycle_level/test_combat_integration contamination during the split                                                                                                                                                                                                               | later   | open   |
| D5-agent-restatement     | 5      | 3x-restatement pattern in transcript-researcher (archive policy, "no spawn"), level-designer (handoff), bug-triage (no game code), skill-researcher (config gating) — state each constraint once                                                                                                                                                                                                                                                                                                              | later   | open   |
| D2-engine-version        | 5      | engine version drift across skills: "Godot 4.6" (greybox, enemy-ai, godot-assets) vs "4.3+" (pixel-lighting) — framework-wide consistency pass                                                                                                                                                                                                                                                                                                                                                                | later   | open   |
| D3-greybox-fps           | 5      | godot-greybox framed entirely as "Godot 4.6 FPS arena" — discloses lock but is genre-locked; product call whether framework is FPS-maker or genre-general                                                                                                                                                                                                                                                                                                                                                     | later   | open   |
| D3-fps-enemy             | 5      | godot-fps-enemy-combat hit/death contract is shooter-generic (camera-agnostic); "fps" over-narrows — rename to godot-shooter-enemy-combat someday                                                                                                                                                                                                                                                                                                                                                             | later   | open   |
| D6-directive-verbose     | 5      | orchestrator data-driven directive (lines 3-10, 8 lines of prose) above routing — condense to ~4 bullets                                                                                                                                                                                                                                                                                                                                                                                                      | later   | open   |
| D7-fix-no-selfcritique   | 5      | framework-audit-fix.md has no self-critique/process-note step (framework-audit + token-audit do)                                                                                                                                                                                                                                                                                                                                                                                                              | later   | open   |

Process note: D8 worked but its open question lived only in the ledger's Last-audit line — the command file never documented D8 (D7-d8-missing). Folded that into a finding. Also: agents over-returned; next pass, cap each gather agent's reply to ~12 findings to keep judging cheap.

### 2026-06-30 — dimensions: D7 (via /framework-feedback)

| id            | bucket | finding + proposed fix                                                                                                                                                                                                                                                                                                                                                                                                         | verdict | status |
| ------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- | ------ |
| D7-loop-index | 5      | forge-local self-improvement COMMAND loop (`framework-audit` cold-scan → `framework-feedback` convo-distil → `framework-audit-fix` apply → `token-audit`) is indexed nowhere; `README.md:15` self-improvement para names only the AGENT loop (bug-triage/skill-researcher/godot-refactor). Fix: add `docs/process/self-improvement.md` mapping the 4 commands + the LEDGER as shared state (or extend README:15 to name them). | later   | open   |

Process note: first `/framework-feedback` run. Distilling THIS session correctly yielded ZERO findings about the just-built on-ramp (already shipped — not re-filed) and one real adjacent gap (the command loop it joins is undocumented). The "don't re-file what's built; surface the real adjacent learning" path works. none else.

### 2026-07-01 — dimensions: D9,D7 (via /harvest-sessions backfill — 5 substantive sessions)

| id                      | bucket | finding + proposed fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | verdict | status |
| ----------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------ |
| D9-gdscript-shadow-lint | 5      | recurring runtime `GDScript::reload` warnings the gate never caught: SHADOWED*GLOBAL_IDENTIFIER (`range`, `sign` — ability_data.gd, directional_force_effect.gd, aim_line_resolver.gd) + CONFUSABLE_LOCAL_DECLARATION (`next_pos` — guard.gd), across sessions 16-44 + 09-47. Harden: add a deterministic lint check (gdstyle rule / `check*\*` in tools/lib/checks.sh) so validate.sh flags shadowed-builtins + confusable locals pre-run. Verify first whether gdstyle already covers it. | later   | open   |
| D7-display-clobber      | 5      | INVESTIGATE: across 09-47 + 16-44 the human repeatedly reports the game's display/window settings reverting ("we keep overwriting my display settings… back to super small, mode !=2"; "the panel needs to load with the default"; "YOU DONT CHANGE"). Determine whether a framework step (materialize/doctor/setup, or the screenshot/verify GD scripts) rewrites project.godot display settings instead of preserving the game's; if game-local config, drop.                             | later   | open   |

Process note: backfill of 5 substantive sessions (+ stubs, no signal). The big recurring friction was ALREADY captured — correctly NOT re-filed: the promote already-in-plugin error recurred (`check_typed_export_nodepath`, 15-44) → confirms D7-promote-already-in-plugin (fixed this week); the Godot-4.6 navmesh bake landmines + stealth-perception stack (16-44/13-43/09-47, "days of enemies not working") are now covered by the WIP skills godot-navmesh-pathing-4-6 / godot-stealth-perception / godot-enemy-ai-headless-smoke. All game bugs (pull/push/enemies/camera/stamina) filtered out. Confirms the "already learned → don't re-file" discipline works across sessions.

### 2026-07-01 — dimensions: D2,D3,D5,D6,D7,D8,D9 (via /framework-audit — weekly cold scan; D1 clean per validate skill-scope "in sync"; D4 clean)

| id                               | bucket | finding + proposed fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | verdict                                                                                                                                           | status  |
| -------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ---- |
| D8-render-tools-missing          | 4      | godot-verify frames render-health as a "godot-dev build / contract" (:71) yet names `tools/verify_arena_render.gd` "**mandatory** for level-render PRs" (:79) + gives a copy-paste Layer-4 `-s tools/verify_render_action.gd` command (:85); NEITHER ships (only the known-insufficient spread-only `verify_render.gd` does). A builder running Layer 4 hits file-not-found. Fix: ship both tools OR mark them "build per contract (not shipped)" + stop presenting the Layer-4 command as runnable. (lib digest verify-render-action.md documents the absent tool too.)                                                                                                                                                                          | fix-now                                                                                                                                           | open    |
| D8-enemy-smoke-orphan-naming     | 4      | godot-enemy-ai-headless-smoke names its headline examples `check_nav_bake.gd`/`test_enemy_health.gd` (:35,:58,:138), but the gate glob only runs `smoke_*`/`play_*` (checks.sh:285) — so `check_*`/`test_*` NEVER join the gate, contradicting the same skill's ":128 a smoke\_\*.gd glob auto-joins the gate." Rename examples to `smoke_nav_bake.gd`/`smoke_enemy_health.gd`.                                                                                                                                                                                                                                                                                                                                                                   | fix-now                                                                                                                                           | open    |
| D9-contamination-check           | 4      | HARDEN (determinism ratchet) — the gate ALREADY EXISTS but is under-scoped: `promote-run.js:53-98` `gameDomainRef` hard-blocks game-domain TOOLS on `res://*.tscn/.tres/.glb` refs (block + genericize hint — the exact mechanism we want), but runs ONLY for `kind==="tools"` and misses codenames / `.gd` paths / `../game` / absolutes / "proven on <game>" provenance. Fix: extract into a shared contamination check, extend the pattern set, run at BOTH seams — (a) `promote` for skills+agents+tools (hard-block + hint + `--force` override, per approved gate design) AND (b) `validate.sh`, the ONLY thing that catches direct-to-plugin authoring (the WIP enemy skills bypassed promote entirely). Supersedes old "path-lint" scope. | fix-now                                                                                                                                           | open    |
| D6-symptom-route-triplication    | 3      | orchestrator.md states "a symptom is never a lookup — route it, don't diagnose" 3× (:26,:37,:119; :119 even cross-refs :37). Keep the operative rule once at :37 (where it's actionable), reduce :119 + :26 to bare pointers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | fix-now                                                                                                                                           | open    |
| D5-handoff-restates-agent-report | 4      | 4 builder agents re-spell the preloaded `agent-report` skill's steps verbatim (godot-visuals:49, godot-assets:51, godot-player:46, godot-dev:79 variant) — "write full report gate-first to handoff file, relay only `<path> — gate PASS                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | FAIL`" duplicates agent-report:15,17,31. Preloaded + known step (handoff) → trim each to "For handoffs, follow the preloaded agent-report skill." | fix-now | open |
| D8-capabilities-registry-drift   | 3      | plugin/tools/CAPABILITIES.md registry omits `playgrade.sh` (central evaluator, invoked by godot-playtester:32,39) + `smoke_scene_errors.sh`, both of which ship in plugin/tools/. An agent reading `forge-facts capabilities` won't see the grader. Add the 2 rows.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | fix-now                                                                                                                                           | open    |
| D8-runtime-smoke-wiring-stale    | 3      | godot-runtime-smoke:244 step 7 tells the builder to hand-wire a runtime-smoke step into validate.sh, but `check_smoke_bots` already globs `tools/smoke_*.gd` (checks.sh:301) → dead step that generates false gap-reports + contradicts the skill's own "do NOT hand-edit tools/". Replace step 7 with "name it `smoke_*.gd` — it auto-joins via check_smoke_bots; just create the file." (residue of done D8-orphaned-smoke.)                                                                                                                                                                                                                                                                                                                    | fix-now                                                                                                                                           | open    |
| D7-harvest-grep-contradiction    | 3      | harvest-sessions.md header (:50 "Search with `rtk grep` + `jq`") + Never bullet (:111 "filter with `rtk grep`/`jq`") still endorse the exact `rtk grep`→`jq` pipe that step 3 (:66-71) forbids ("rtk's grep filter mangles JSON"). Reword both summary lines to "filter with `jq select(...)` directly." (residue of done D7-jq-grep-recipe.)                                                                                                                                                                                                                                                                                                                                                                                                     | fix-now                                                                                                                                           | open    |
| D3-verify-subviewport-lock       | 5      | godot-verify Layer 3/4 capture "the **SubViewport** (the pixel-art rig)" (:79,:95,:116-119); the framework also spans HD (godot-mesh-import-hd/-hd-material-import) where a non-pixel game renders 3D to the root viewport → the capture instructions silently assume a pixelation rig. Scope: capture SubViewport when the game uses a pixelation rig, else root. Park for the HD-vs-pixel branch.                                                                                                                                                                                                                                                                                                                                               | later                                                                                                                                             | open    |
| D5-builder-restates-verify       | 5      | builder agents restate preloaded `godot-verify` content — the interactive-acceptance paragraph (godot-dev:73, godot-visuals:45 ≈ godot-verify:143-154) + tscn/Transform3D authoring rules (godot-dev:61-63 ≈ godot-verify:160-163). COLLIDES with the "verify gates stay inline (reliability>DRY)" principle → the gate one-liner stays inline regardless; human decides if the LONG paragraph + technical-rule copies are redundant vs deliberate reinforcement.                                                                                                                                                                                                                                                                                 | later                                                                                                                                             | open    |
| D9-audit-fanout                  | 5      | STRIP (hypothesised, NEEDS measurement): framework-audit.md:61 "one sub-agent per dimension (parallel)" is context-anxiety scaffolding; under 1M-context Opus the whole plugin/ spine fits one context, so a single high-effort pass may match the 9-agent fan-out at lower cost. Model-upgrade ritual: run `/framework-audit all` single-agent vs fan-out once, compare findings+tokens, keep fan-out only if it still wins.                                                                                                                                                                                                                                                                                                                     | later                                                                                                                                             | open    |
| D9-greybox-eye-harden            | 5      | HARDEN (partial): godot-greybox:47 ships a "self-audit BY EYE" gate; a subset of the nine principles is deterministic (nav-graph reachability, dead-end count, missing NavigationRegion3D, cover-node count) — sibling godot-gridmap-level already hardened its by-eye out (:23,:52). Draft `check_greybox_reachability`; genuinely visual principles (sightline, mystery) stay by-eye.                                                                                                                                                                                                                                                                                                                                                           | later                                                                                                                                             | open    |

Process note: weekly cold scan; parallel one-agent-per-dimension-cluster gather, lead judged. LOAD-BEARING NARRATIVE: last week's harvest note said the new WIP skills godot-navmesh-pathing-4-6 / -stealth-perception / -enemy-ai-headless-smoke now "cover" the enemy bugs — this audit finds those exact 3 are the spine's biggest fresh contamination (D2-enemy-ai-codenames): the river's inflow became the newest drift, so harvest→audit hand-off works but new skills ship dirty. Correctly NOT re-filed: D8-verify-gate-gap (confirmed still open — godot-enemy/-ranged-combat/-vfx still lack the block), D2-library-records (the D2 sweep CONFIRMED it's ~10 addon/tool files incl. "DiceOfFate" in sprint-view-model-feel.md + `pushable_enemy.gd` in the new library/addons/enemy-ai.md — strengthens the existing finding, no new id). Correctly SKIPPED: D5-hermes-input-block (= already-skipped D5-research-intake), D5-form-fallback-condition (condition-only/slotted dup, actions per-agent), enemy-ai FPS-genre aside (the SubViewport rig IS the framework convention per godot-main-scene, so only "(FPS)" over-narrows — one aside, not worth churn). Loop tweak: D9-contamination-check is itself a self-improvement — a shared contamination gate would turn the recurring fuzzy D2 sweep deterministic; recommend building it early so future audits only judge proper nouns.

### 2026-07-01 — root-cause follow-up (human-steered deep-dive): the promotion/authoring SEAM — dimensions D7,D9

Human insight: D2-enemy-ai-codenames / D2-library-records are SYMPTOMS; the disease is that "agnostic" is a rubric humans eyeball, never enforced at authoring or promotion — and the same gap lets promote-off-a-draft happen. Traced the full skill/tool creation path (promotion.md, skill-researcher.md:42, promote-run.js, promotions-store.js). These prevent the D2 symptoms recurring.

| id                               | bucket | finding + proposed fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | verdict | status |
| -------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- | ------ |
| D7-promote-board-vapor           | 4      | `promotions-store.js addPromotion` (:53) files a promote request with NO existence/wired check → drafts & proposals get boarded (e.g. `check_scene_export_assigned`, `flake_check` pointing at `.xenodot/handoffs/*.sh` / a report proposal that were never authored), then `promoteOne` skips them "not found at <src>" (promote-run.js:118) — confusing, and it lets agents skip the local-first ratchet. Fix: `addPromotion` validates `locate(kind,name,game).src` exists at REQUEST time (+ for tools, that it's gate-wired: referenced by validate.sh / a `check_*` / a `smoke_*`/`play_*` glob), rejecting with "author it locally as a real, working, gate-wired tool and prove it in THIS game first, then file." Enforces the local-first flow promotion.md states but nothing checks. | fix-now | open   |
| D7-agnostic-authoring-convention | 4      | PREVENTION (both authoring paths): no convention separates game-local artifacts (may name THIS project) from framework artifacts (agnostic). skill-researcher.md:42 template bakes in `## Project conventions (file paths, node names, defaults for THIS project)` with no genericize-on-promote note; Path B (in-house lesson capture straight into plugin/ — the WIP enemy skills) has ZERO checklist and is the dominant contamination source. Fix: extend promotion.md criterion 1 with a **placeholder standard** (`<enemy>.gd`, `res://levels/<level>.tscn`, "a real project's patrol enemy"; provenance stated agnostically, concrete evidence stays game-local) + note in skill-researcher's template that `## Project conventions` is game-local-only and MUST genericize on promote.   | fix-now | open   |
| D7-tool-domains-doc              | 3      | `promote-run.js:129` cites `docs/process/promotion.md → "Tool domains"` but that section does NOT exist (verified). Add it: document game-domain vs universal tools (mirror `UNIVERSAL_RES` / `gameDomainRef` in code) so the gate's cited doc is real and "what's a game tool vs an every-game tool" is written down.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | fix-now | open   |
| D7-lesson-record-template        | 4      | Human ask: "when creating a tool, create a lesson-learned on the framework, verbatim super simple." The `plugin/library/tools/*.md` digests ARE the de-facto tool lessons but are game-coupled (D2-library-records). Fix: define a tiny AGNOSTIC lesson-record template (what/why/gotcha/universal-vs-game, ~6 lines) for `plugin/library/tools` (+ addons); concrete game facts stay in the GAME's library. Pairs with D2-library-records' genericize-vs-archive decision.                                                                                                                                                                                                                                                                                                                      | fix-now | open   |
| D7-fix-prunes-on-done            | 3      | The ledger header mandates "prune resolved rows back to the Last-audit line" but nothing enforces it, so `/framework-audit-fix` marks rows `done <date>` and leaves them → done rows accumulate as distraction (this pass pruned 14 by hand). Fix: `framework-audit-fix.md` REMOVES a resolved row (and a fully-resolved entry) instead of stamping `done`, and carries the one-line-per-id summary into its commit message — git is the "what was fixed" record, not the ledger. Removes the need for a separate changelog.                                                                                                                                                                                                                                                                     | fix-now | open   |

Process note: root-cause deep-dive (human-steered, not cold-scan). Key discovery — the contamination GATE mechanism already exists (`gameDomainRef`, hard-block + hint + "parameterize then re-promote"), just tools-only + res://-only; the fix is GENERALIZE, not invent (folded into D9-contamination-check). CRUCIAL nuance: the WIP enemy skills were authored DIRECT-TO-PLUGIN, bypassing promote entirely — so a promote-only gate can't catch them; the check MUST also run in validate.sh (both seams). The D2-\* rows stay as one-time debt cleanup; D9-contamination-check + D7-agnostic-authoring-convention are what stop the recurrence. Loop tweak: consider a D10 "seam integrity" dimension (do the framework's OWN process gates — promote, materialize, validate — enforce the conventions the docs state?) — this whole class (eyeballed-not-enforced) sits between D7 (command text) and D9 (harness), and the audit found it only because a human pushed past the symptom.
