# How to make 3D Games in Godot (3D handling foundations) — transcript digest

**Source** — `godot-3d-handling-foundations.md` (the raw, now in `transcripts/archive/godot-3d-handling-foundations.md`). Broad "intro to 3D in Godot" overview video; functionality + the "3 pillars of 3D graphics" (assets, lighting, environment). Built in Godot 4.3.
**Why harvested** — we're about to build in the "How to make 3D Games in Godot" domain (general 3D handling foundations: importing/wiring models, collision, animation, the rendering pillars).

**Points**

| #   | Point (technique/claim)                                                                                                                                                                                                                                                                       | Valid for our stack? | Already learned?           | Where / gap                                                                                                                                                                                                                              | Verdict                                                                            |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| 1   | 2D nodes map ~1:1 to 3D (Node3D, CharacterBody3D, Area3D, RayCast3D…); programming (is_colliding, position±axis) is the same with a Z axis added                                                                                                                                              | holds                | covered (general literacy) | implicit across all godot-\* skills + entities/                                                                                                                                                                                          | no action                                                                          |
| 2   | 1 unit = 1 metre; most online assets are scaled to that; tune with `root_scale` on import                                                                                                                                                                                                     | holds with caveat    | partial                    | `godot-mesh-import-pixel-art` (scale-to-footprint), CLAUDE.md art-kind router                                                                                                                                                            | no action — covered enough                                                         |
| 3   | Greybox/blockout first with CSG nodes (CSGBox + subtractive CSG + CSGCombiner, `use_collision`), then replace with assets                                                                                                                                                                     | holds with caveat    | partial                    | We greybox, but with **flat-colour BoxMesh** (CLAUDE.md art-kind: "Greybox stage") + GridMap, NOT CSG. Cyclops eval (`library/cyclops-level-builder.md`) already covers the hand-built path. CSG's runtime-cost/bake nuance not captured | no action — our greybox path is deliberate; CSG is an alternative we chose against |
| 4   | terrain 3D extension for organic/outdoor terrain sculpting (no built-in Godot solution)                                                                                                                                                                                                       | out of scope         | n/a                        | not on roadmap; outdoor terrain not a current build                                                                                                                                                                                      | Later                                                                              |
| 5   | Use the preview sun/env in editor; "Add sun to scene" + "Add environment to scene" so the running game is lit (editor preview ≠ runtime)                                                                                                                                                      | holds                | covered                    | `godot-pixel-lighting` (DirectionalLight3D + WorldEnvironment), `godot-verify` (editor viewport is NOT verification)                                                                                                                     | no action                                                                          |
| 6   | A character controller's needs depend on the game; start simple. Author shares a free CC0 "protoc" first-person prototyping controller addon                                                                                                                                                  | holds with caveat    | partial                    | We have `entities/player` + `godot-composition`; ours is third-person-ish orthographic, not the FP protoc. No reusable "prototyping controller" abstraction, but composition covers the build approach                                   | Later — we build our own controller                                                |
| 7   | 3D physics mirrors 2D (StaticBody3D/RigidBody3D + CollisionShape3D); non-uniform collider scaling warns + breaks; prefer **Jolt** physics engine (built-in & enableable in 4.4+, addon in 4.3)                                                                                                | holds                | partial                    | `design/collision-shapes.md` covers shapes; **Jolt engine choice is a project-config decision we have not made/recorded** (we're on 4.6, so Jolt is built-in)                                                                            | gap — see Recommended next                                                         |
| 8   | glTF is the recommended format; `.glb` (binary) preferred; `.blend` imports via installed Blender; re-export odd formats through Blender                                                                                                                                                      | holds                | covered                    | `godot-mesh-import-pixel-art` + `library/model-sources.md` (glTF-binary at assets/models/)                                                                                                                                               | no action                                                                          |
| 9   | Godot treats a glTF as a scene; use Advanced Import to skip nodes, override/extract materials (use-external), handle embedded/packed textures; `mesh_compress`, `root_scale`, normal-map invert-Y                                                                                             | holds                | partial                    | `godot-mesh-import-pixel-art` covers NEAREST + Make-Unique + scale; **Advanced Import material override/extract, embedded-texture handling, normal-map invert-Y are not in the skill**                                                   | gap — see Recommended next                                                         |
| 10  | PBR StandardMaterial3D map types: albedo/metallic/roughness/normal/AO/emission; channel-pack AO+rough+metal into one ORM texture (ORMMaterial3D); specular-map workflow unsupported                                                                                                           | holds with caveat    | gap                        | We use flat-colour boxes / tileable albedo (CLAUDE.md art-kind router) and low-res SubViewport — full PBR/ORM is mostly out of scope for the pixel-art look, but normal/emission could matter                                            | Later — not needed for current pixel-art look                                      |
| 11  | Three ways to use an imported model: **inherited scene** (fragile, couples logic to art — avoid), **nested scene** (recommended default — own scene wraps the model), **make-local** (full control but bloats scene with mesh data in 4.3)                                                    | holds                | gap                        | `godot-mesh-import-pixel-art` does "1:1 swap of greybox node" but does **not** state the inherited-vs-nested-vs-make-local decision (separate function from visuals; nested = default). Reinforces our composition principle             | gap — see Recommended next                                                         |
| 12  | Collision for imported models: prefer simple shapes; combine multiple shapes for accuracy; or auto-generate (single-convex / etc.) from mesh via "Create Collision Shape"; **Make-Unique on duplicated CollisionShape resources**                                                             | holds                | partial                    | `design/collision-shapes.md` + `godot-mesh-import-pixel-art` (Make-Unique on mesh materials). Make-Unique on **duplicated collision-shape resources** + auto-generate-from-mesh not explicitly captured                                  | gap — see Recommended next                                                         |
| 13  | Animated characters: skeleton+meshes+AnimationPlayer auto-created from glTF; prefer model and animations in **separate glTF** files, import anim-only files as **AnimationLibrary**, load into your own AnimationPlayer; **retargeting** reuses anims across non-identical rigs (e.g. Mixamo) | holds                | gap                        | No skill or doc covers animation import / AnimationLibrary / retargeting at all                                                                                                                                                          | gap — see Recommended next (only if we add animated characters this build)         |
| 14  | Import name-suffix tricks: `-noimp` (skip node), `-col`/`-convcol`/`-colonly` (model custom colliders), `-loop` (auto-loop anim); plus import scripts for repeatable automation                                                                                                               | holds                | gap                        | Not captured anywhere; pairs with #9/#12                                                                                                                                                                                                 | Later — power-user automation, not blocking                                        |
| 15  | WorldEnvironment background (custom color vs procedural Sky vs PhysicalSky vs Panorama/HDRI from Poly Haven) drives ambient + reflected light even when not visible; energy multiplier; tune ambient sky contribution                                                                         | holds with caveat    | covered                    | `godot-pixel-lighting` (Sky/Color ambient fill on SubViewport Environment)                                                                                                                                                               | no action                                                                          |
| 16  | Three light types: DirectionalLight3D (sun), OmniLight3D (bulb/radius), SpotLight3D (cone); start with sun + place Omni/Spot for color pops & fill; enable shadows sparingly (cost); SSAO in env                                                                                              | holds                | covered                    | `godot-pixel-lighting` (one sun, hard shadows, sky-ambient balance)                                                                                                                                                                      | no action                                                                          |
| 17  | Tonemap (Filmic default / ACES / AgX in 4.4) shapes the contrast curve & stops highlight clipping; adjustments: color-correction LUT or a GradientTexture1D for highlight/shadow tinting; camera attributes (exposure, auto-exposure)                                                         | holds with caveat    | covered                    | `godot-pixel-lighting` chooses **Filmic over ACES/AgX** deliberately + fixed exposure (auto-exposure would fight pixel readability)                                                                                                      | no action — our convention already picks the opposite trade                        |
| 18  | Render quality knobs are spread across Project Settings: MSAA (anti-aliasing), environment advanced, light & shadows                                                                                                                                                                          | holds with caveat    | partial                    | `godot-3d-pixelation` owns the look via SubViewport downscale; **MSAA fights crisp low-res pixels** — a caveat worth noting but not a gap to fill                                                                                        | no action                                                                          |
| 19  | Camera: orthogonal projection + size for isometric (vs perspective FOV)                                                                                                                                                                                                                       | holds                | covered                    | `godot-camera-rig` (orthographic fixed-angle is mandatory)                                                                                                                                                                               | no action                                                                          |

**Recommended next** — gaps that matter for building 3D-handling foundations now, one line each:

- glTF model-import depth (Advanced Import: per-node skip, material override/extract, embedded-texture handling, normal-map invert-Y) + the **inherited vs nested vs make-local** decision + Make-Unique/auto-generate on imported colliders → **skill-researcher** (extend or evaluate alongside `godot-mesh-import-pixel-art`; points 9, 11, 12). This is the most load-bearing gap for "handling 3D" — our model skill stops at NEAREST+scale and doesn't cover the import-window workflow or the scene-structure decision.
- Jolt physics engine choice unrecorded → **game-designer** (a project-config decision): on Godot 4.6 Jolt is built-in/enableable; decide whether to switch `physics/3d/physics_engine` to Jolt and record it in CLAUDE.md conventions (point 7).
- (Only if animated characters are in scope this iteration) animation import workflow — separate model/anim glTFs, AnimationLibrary, retargeting → **skill-researcher** (point 13). Park if no animated character is being built now.

**Later** — valid points parked (not needed for the current build):

- terrain 3D / organic outdoor terrain (#4) — not on roadmap.
- A reusable "prototyping character controller" abstraction (#6) — we build our own via composition.
- Full PBR/ORM channel-packing & normal/emission maps (#10) — mostly out of scope for the low-res pixel-art look.
- Import name-suffix tricks + import scripts (#14) — power-user automation; revisit if model volume grows.
- CSG-based greyboxing (#3) — we deliberately greybox with flat BoxMesh + GridMap; CSG already weighed against in `library/cyclops-level-builder.md`.
